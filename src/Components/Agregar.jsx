import React from 'react';

const Agregar = () => {
  const handleClick = () => {
    // Lógica al hacer clic
  };

  return (
    <div>
      <button title="Agregar al carrito" onClick={handleClick}>
        Agregar al carrito
      </button>
    </div>
  );
};

export default Agregar;